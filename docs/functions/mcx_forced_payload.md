---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "mcx_forced_payload function - terraform-provider-jamfplatform"
subcategory: ""
description: |-
  Builds a macOS managed (forced) preferences payload for a Custom Settings configuration profile.
---

# function: mcx_forced_payload

Encodes a set of application preferences into a macOS managed (forced) preferences payload. Pass the result to a macOS configuration profile's `payloads` so the settings are delivered to enrolled Macs as non-overridable defaults. Preference values keep their natural types: whole numbers become integers, fractional numbers become reals, booleans and strings map directly, lists become arrays, and nested objects become dictionaries. Whole numbers above 2^53 lose integer precision (the provider carries numbers as floating point); this does not affect normal preference values.

## Example Usage

```terraform
# Build a macOS managed (forced) preferences payload and deliver it via a
# configuration profile. Whole numbers become integers, fractional numbers
# reals, booleans/strings map directly, lists become arrays, nested objects
# become dictionaries.
resource "jamfplatform_pro_macos_configuration_profile" "app_settings" {
  general = {
    name = "Example App Configuration"
    payloads = provider::jamfplatform::mcx_forced_payload("com.example.app", {
      RotateWithinHours = 24
      AdminBase         = "https://admin.example.com"
      Browsers          = ["edge", "chrome", "firefox"]
    })
  }

  scope = {
    targets = {
      all_computers = true
    }
  }
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
mcx_forced_payload(preference_domain string, preferences dynamic) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `preference_domain` (String) The application preference domain the settings apply to, e.g. `com.example.app`.
1. `preferences` (Dynamic) An object whose keys are preference names and whose values are the preference values (strings, numbers, booleans, lists, or nested objects).
